# Jenkins uses the dockerdind service (full Docker in Docker); DOCKER_HOST is set in docker-compose.yml.

J_HTTP_PORT=8086

G_HTTP_PORT=3000
G_USER=admin
G_PASS=admin123
G_EMAIL="admin@test.com"
G_REPO_NAME=terraform-gitea-repo
